/**
 * 删除一个数组中的最后的一个元素，并且返回这个元素。
 *
 * array.pop()
 * 
 */

var arr = [1, 2, 3, 4, 5, 6];

console.info(arr); //[ 1, 2, 3, 4, 5, 6 ]
var popEle = arr.pop();
console.info(popEle); //6
console.info(arr); //[ 1, 2, 3, 4, 5 ]
